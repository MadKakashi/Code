#include<stdio.h>
#include<stdlib.h>

int main(){ 
    int i; //keep track of the rows
    int j; //keep track of the columns
    int n; //stores the number of rows or columns
    int a; //stores the current value of the element of matrix
    int counter= 3;
    // if (n==3) counter = 3;
    // if (n==4) counter = 6;
    scanf("%d",&n); //inputing the number of rows or columns

    for (i=0;i<n;i++){
        for (j=0;j<n;j++){
            scanf("%d",&a);
            

            

        }
    }
    return 0;
}

   