#include <iostream>

using namespace std;

int main()
{
    float radius;
    cout << "Enter the radius:";
    cin >> radius;
    float area =(22/7.0) * (radius) * (radius);
    cout << "Area of the cirle is " << area << endl;
    return 0;
}