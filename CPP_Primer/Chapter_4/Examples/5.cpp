#include<iostream>
#include<string>
#include<vector>

using namespace std;

int main(){
    // vector<int>what = {1,2,3};

    // for (auto &a : what){
    //     (a%2==0)?a*=2:a;
    // }

    // for (auto a : what){
    //     cout << a << endl;
    // }


    // if (grade>90)cout << "high pass";
    // else if (grage<60) cout << "fail";
    // esle if (grade<75) cout << "low pass";
    // else cout << "pass";




    // string p1 = s +(s[s.size()-1]=='s'?"":"s");
    string f = "this";
    string s1 = f + 'c';
    cout << s1;
    return 0;
}