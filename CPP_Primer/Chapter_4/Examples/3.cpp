#include<iostream>
#include<string>
#include<vector>

using namespace std;

int main(){
    //first element will be skipped and a negative integer will also be in the output


    //checks the value of ptr variable and ig that is true then it checks the value at the ptr and then increment the ptr varaible -legal
    //check the value of the ival and then increment it then also check the valeu fo the incremented variable ival and then checkit
    //illgaa because ival is used in both left hand side and right hand side and which is undefined behaviour which change the value of the varialb ewhich is used on the both sides 
    return 0;
}