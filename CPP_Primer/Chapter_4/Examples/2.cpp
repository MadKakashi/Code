#include<iostream>
#include<string>
#include<vector>

using namespace std;

int main(){
    //the right hand exp is evaluated in AND operator if the first operation if true
    //the right hand exp is evaluated in OR operator if the right hand operator is false

    //the if statement returns true if the pointer is valid and in this case it is valid
    // and second statement returns true if the value of *cp is not zero

    // int n;
    // while(cin>>n && n!=42);

    // if (a>b && b>c && c>d)

    //first the condn i!=j which in turn true a value 1 or 0 depending whethter it is true or false
    //then the resulting value (1 or 0) is compared if it is greager than k or not

    return 0;
}